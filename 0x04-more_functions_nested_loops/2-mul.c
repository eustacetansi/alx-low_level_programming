#include "main.h"
#include <stdio.h>

/**
 * function that multiplies two integers
 *
 * Return: Always o.
 */
int mul(int a, int b)
{
	int result;
	result = a*b;
	_putchar("%d", result);
}
int main()
{
	mul();
	return (0);
}
